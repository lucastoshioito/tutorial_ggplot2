# Adding a plot

This folder is a personal workspace for reusable figures.

1. Copy `_template.qmd` to a short, descriptive filename.
2. Keep the data preparation, plotting code, and useful notes together in that file.
3. Add the page to `_quarto.yml` only when it is ready to appear on the website.
